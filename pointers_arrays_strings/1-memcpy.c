#include "main.h"
/**
 * _memcpy - copies memory area
 * @dest: memory
 * @src: meomry
 * @n: number of bytes
 *
 * Return: copied memory
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	int i = 0;
	int b = n;

	for (i < n; a++)
	{
		dest[i] = src[i];
	}
	return (dest);
}
